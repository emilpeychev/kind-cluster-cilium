apiVersion: v1
kind: Secret
metadata:
  name: harbor-helm-repo
  namespace: argocd
  labels:
    argocd.argoproj.io/secret-type: repository
# Template secret: do NOT commit real credentials. Create this secret at install time.
stringData:
  name: harbor-helm
  url: https://harbor.local
  type: helm
  enableOCI: "true"
  username: "robot$argocd"
  # password: <set at install-time from .harbor-robot-pass.env>
  insecureSkipServerVerification: "true"
