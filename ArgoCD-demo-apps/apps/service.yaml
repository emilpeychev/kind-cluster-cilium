apiVersion: v1
kind: Service
metadata:
  annotations:
    argocd.argoproj.io/sync-wave: "3"
  finalizers:
    - resources-finalizer.argocd.argoproj.io
  name: demo-app1
  namespace: demo-apps
spec:
  selector:
    app: demo-app1
  ports:
    - port: 80
      targetPort: 80
  type: ClusterIP
